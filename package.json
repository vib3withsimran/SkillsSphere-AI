{
  "name": "skillsphere-ai-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "cd client && npm install && cd ../server && npm install && cd ../ai-ml && npm install",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run client\" \"npm run server\""
  },
  "dependencies": {
    "google-auth-library": "^10.6.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "concurrently": "^8.2.2",
    "postcss": "^8.5.12",
    "tailwindcss": "^4.2.4"
  }
}
